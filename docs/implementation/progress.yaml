# Implementation progress — 42wasd-mc
#
# Source of truth for the status of each build phase in the reading-order
# manifest (docs/reference-design/_sequence.yaml). Status values:
#   done | in-progress | not-started | blocked | deferred
#
# Key by the part slug + section slug, e.g.
#   03-step-by-step-implementation:
#     deploy-cockroachdb-and-nakama: done
# You may set a status on a part, a phase, or a nested section; any section
# without an explicit status inherits its nearest ancestor's.

03-step-by-step-implementation:
  decide-names-before-deploying: done
  create-repository-structure: done
  create-kubernetes-namespaces: done
  install-openkruisegame: done
  install-keda-and-observability: done
  deploy-cockroachdb-and-nakama: in-progress  # CockroachDB done; Nakama image bump + certs pending
  # Phase 0 — Decide names before deploying
  # Phase 1 — Create repository structure
  # Phase 2 — Create Kubernetes namespaces
  # Phase 3 — Install OpenKruiseGame
  # Phase 4 — Install KEDA and observability stack
  # Phase 5 — Deploy CockroachDB and Nakama
  # Phase 6 — Deploy Velocity
  # Phase 7 — Deploy the Paper lobby
  # Phase 8 — Install TAB
  # Phase 9 — Add ViaVersion and ViaBackwards
  # Phase 10 — Deploy the Forge 1.20.1 fantasy runtime
  # Phase 11 — Define the runtime catalog
  # Phase 12 — Define map metadata
  # Phase 13 — Build the World Controller
  # Phase 14 — Build NetworkBridge for Velocity
  # Phase 15 — Implement friends and parties
  # Phase 16 — Implement /join <friend>
  # Phase 17 — Implement pending cross-runtime invites
  # Phase 18 — Publish Modrinth Server Projects
  # Phase 19 — Add packwiz CI
  # Phase 20 — Add exact world/dimension TAB information
  # Phase 21 — Implement the glitch/random portal
  # Phase 22 — Add mc-router
  # Phase 23 — Add idle sleep
  # Phase 24 — Add Agones only for session worlds
  # Phase 25 — Add AI proximity chat
  # Phase 26 — Add object storage
  # Phase 27 — Community map upload pipeline
  # Phase 28 — Backups
  # Phase 29 — Monitoring
  # Phase 30 — Rollout order