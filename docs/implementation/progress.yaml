# Implementation progress — 42wasd-mc
#
# Source of truth for the status of each build phase in the reading-order
# manifest (docs/reference-design/_sequence.yaml). Status values:
#   done | in-progress | not-started | blocked | deferred
#
# Key by the part slug + section slug, e.g.
#   03-step-by-step-implementation:
#     deploy-cockroachdb-and-nakama: done
# You may set a status on a part, a phase, or a nested section; any section
# without an explicit status inherits its nearest ancestor's.

03-step-by-step-implementation:
  # Phase 0 — Decide names before deploying
  # Phase 1 — Create repository structure
  # Phase 2 — Create Kubernetes namespaces
  # Phase 3 — Install OpenKruiseGame
  # Phase 4 — Deploy CockroachDB and Nakama
  # Phase 5 — Deploy Velocity
  # Phase 6 — Deploy the Paper lobby
  # Phase 7 — Install TAB
  # Phase 8 — Add ViaVersion and ViaBackwards
  # Phase 9 — Deploy the Forge 1.20.1 fantasy runtime
  # Phase 10 — Define the runtime catalog
  # Phase 11 — Define map metadata
  # Phase 12 — Build the World Controller
  # Phase 13 — Build NetworkBridge for Velocity
  # Phase 14 — Implement friends and parties
  # Phase 15 — Implement /join <friend>
  # Phase 16 — Implement pending cross-runtime invites
  # Phase 17 — Publish Modrinth Server Projects
  # Phase 18 — Add packwiz CI
  # Phase 19 — Add exact world/dimension TAB information
  # Phase 20 — Implement the glitch/random portal
  # Phase 21 — Add mc-router
  # Phase 22 — Add idle sleep
  # Phase 23 — Add Agones only for session worlds
  # Phase 24 — Add AI proximity chat
  # Phase 25 — Community map upload pipeline
  # Phase 26 — Backups
  # Phase 27 — Monitoring
  # Phase 28 — Rollout order