# thirdparty

Third-party resources, vendored code, or external references used by the `42wasd-mc` project.

Place downloaded dependencies, vendored libraries, or documented external references here so they stay separate from first-party code.