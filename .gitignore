# Python & Virtualenv
.venv/
__pycache__/
*.pyc

# uv Python version (pin is in projects/.python-version)
.python-version

# MkDocs Local Build Artifacts
site/
.cache/

# Minecraft world data (large binaries; managed outside Git)
world-data/
world-data-export-*.tar.gz

# Secrets — never commit keys, certs, or generated secrets
*.key
*.pem
*.p12
*.pfx
.env
.env.*
!*.example
secrets.generated/

# kubeconfig
kubeconfig
*.kubeconfig