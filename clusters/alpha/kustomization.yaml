# Alpha cluster overlay (alpha-games-prd).
# Aggregates the shared platform components deployed into the games namespace.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: prd-games-42wasd-admin
resources:
  - namespace.yaml
  - lobby
  - velocity
  - nakama
  - cockroachdb